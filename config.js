// Paste your Firebase web-app config here (Firebase console → Project settings → Your apps).
// Until you do, the site runs fine in device-only mode: words save in the browser,
// and Export / Import JSON still works.
window.FIREBASE_CONFIG = {
  apiKey: "PASTE_ME",
  authDomain: "PASTE_ME.firebaseapp.com",
  projectId: "PASTE_ME",
  appId: "PASTE_ME",
};
// Leave false in production. True connects to local Firebase emulators for development.
window.USE_EMULATORS = false;
